# Pinned to the 1.x series actually installed/tested in this environment (the project
# started against 0.3.x, but the environment has since moved to LangChain 1.x — the
# agent module (factory_floor/agent.py) uses langchain.agents.create_agent, only
# available in 1.x).
langchain>=1.3.0
langchain-core>=1.5.0
langchain-openai>=1.5.0
langchain-community>=0.4.0
langchain-chroma>=1.1.0
langchain-text-splitters>=1.1.0
chromadb>=1.5.0
pypdf>=5.0.0
streamlit>=1.38.0
python-dotenv>=1.0.1
requests>=2.32.0
tiktoken>=0.8.0
jupyter>=1.1.0
ipykernel>=6.29.0

# Computer vision — defect classification (item 05, MVTec AD-based)
Pillow>=10.0.0
scikit-learn>=1.5.0
torch>=2.2.0
torchvision>=0.17.0
huggingface-hub>=0.24.0  # dataset download only (download_defect_images.py), no model hosting involved

# Tracing/observability (item 08 notebook) — imported directly by factory_floor/tracing.py
# and factory_floor/rag.py (Client.get_run_url()/LangChainTracer), so declared explicitly
# instead of relying on it arriving transitively via langchain-core. Upper-bounded because
# the run-url/share-run APIs this project uses are deprecated with removal after 2027-01-31.
langsmith>=0.11.0,<1.0

# Already used by vision.py's structured-output models and now also by safety.py, but never
# declared — was only ever present transitively via langchain-core.
pydantic>=2.7
